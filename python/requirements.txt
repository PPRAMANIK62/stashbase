# StashBase Python sidecar dependencies. Installed into a venv at
# `<project>/python/.venv.nosync/` by the `setup:python` npm script.
#
# MFS owns the embedded catalog, chunk index, and search implementation. Pin
# the exact reviewed upstream revision: this integration uses its public MFS,
# Internal namespace, Processor, Chunker, and Embedder interfaces.
#
# The remaining packages are used directly by the StashBase adapter:
#   - openai:       OpenAI-compatible embedding client for the fixed
#                   text-embedding-3-small family.
#   - numpy:        vector validation in the provider adapter (also pulled
#                   transitively by MFS).
#   - blake3:       stable opaque namespace key from Folder comparison identity.
#
# Optional local PDF/OCR extraction dependencies live in
# `requirements-extract.txt`. Keep them out of the default venv and
# packaged daemon so normal setup/release avoids their document-parser and
# computer-vision payloads.
#
mfs @ git+https://github.com/liliu-z/mfs.git@357fe2525d663f8d6b510db16b28c209f42f11b9
openai>=1.0
numpy>=1.26
blake3>=1.0
