# Capability Ledger

This ledger is the migration's progress record. It does not replace
[Journey Coverage](../../../code-review/journey-coverage.md), which remains the
canonical evidence map for Shipping behavior.

Use these statuses only:

- **Not assessed** — current behavior and evidence have not been inspected.
- **Specified** — contracts, required results, and current evidence are pinned.
- **Building** — replacement implementation exists but has not passed its gate.
- **Proven** — mapped evidence passes against the replacement.
- **Cut over** — the production renderer uses the proven replacement.
- **Blocked** — a named decision, Interface, or evidence gap prevents progress.

Passing a broad command alone does not make a row Proven. Add links to the
exact tests or release checks that assert the required result as work begins.

Disposition is **Retain**, **Change**, **Remove**, or **Unresolved**. A capability
may split into sub-rows when its observable behaviors have different
dispositions. `Building`, `Proven`, and `Cut over` rows require every field;
docs validation must reject incomplete rows at those statuses.

| Capability                   | Journeys | Disposition / decision                                                                                                                                                                                                                                                                                                                                                | Owning contracts                                                                                                   | Replacement Interface                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Compatibility impact                                                                                                                                                                                                                                                                                                                                                                                                                              | Status       | Exact evidence / blocker / deletion condition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Bootstrap and onboarding     | J01      | Retain / [0002](../decisions/0002-feature-runtime-and-protocol-ownership.md), [0005](../decisions/0005-responsive-electron-runtime.md), [0007](../decisions/0007-failure-recovery-and-test-foundation.md), [0010](../decisions/0010-sandboxed-renderer-and-isolated-build.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md)                | Renderer Workspace; Settings and Config; Window Lifecycle                                                          | `LibraryWelcome` / `LibrarySidebar` → `LibraryApi`; `WorkspaceSessionRuntime` → authorized `stashbase.workspaceSession`; `stashbase.library.chooseFolder`; `GET /api/library`; `POST /api/library/folders/open`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Additive replacement HTTP routes and exact `app://renderer` origin policy; legacy folder routes remain until cutover; renderer receives no window identity; unsupported session versions are ignored                                                                                                                                                                                                                                              | Building     | Task 23 first-folder evidence and Task 28 membership-authorized returning-workspace restoration pass the focused component, runtime, protocol, Electron-boundary, and live `app://` smoke evidence listed in [Subphase 2](subphases-2-bootstrap-and-workspace.md). Remaining J01 first-value, Settings, Retrieval, and final journey evidence are deferred to their owning tasks.                                                                                                                                                                                                                                      |
| Library and folders          | J02      | Retain / [0002](../decisions/0002-feature-runtime-and-protocol-ownership.md), [0004](../decisions/0004-runtime-scope-shared-identities-and-navigation.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md)                                                                                                                                    | Renderer Workspace; File Transactions; Data Lifecycle; Window Lifecycle                                            | `LibrarySnapshot` query key `['library', 'membership']`; `WorkspaceRuntime` with immutable folder/generation scope, serializable tree state, and folder-owned query prefix; `WorkspaceSessionRuntime` with a strict v1 persistence adapter; `FileTree` → `FilesApi` with folder-explicit entry create, rename, and delete, inline naming, one tree context menu, and confirmed deletion; validated library, files, workspace-session, and entry-mutation protocols; typed Electron folder release coordination and authoritative reconciliation                                                                                                                                                                                                                                                                                            | First-folder registration and folder removal use additive replacement routes backed by existing server membership and cleanup authority; file listing and workspace-session persistence have strict replacement contracts; legacy folder and file endpoints stay compatible until cutover                                                                                                                                                         | Building     | Tasks 23–28 prove settled welcome and complete membership states; native open/create/removal and local failure handling; authoritative runtime switching; accessible bounded tree rendering with folder-explicit create, rename, and delete that retire open documents first; cross-window release and target-only loss eviction; plus first-window-only durable restore, reload-local session retention, membership-gated folder reopening, stale-restore rejection, and bounded tree/tab/sidebar presentation restoration. Exact focused evidence is listed in [Subphase 2](subphases-2-bootstrap-and-workspace.md). Final J02 journey, accessibility, and performance evidence remains deferred. |
| Documents and editing        | J03      | Retain / [0002](../decisions/0002-feature-runtime-and-protocol-ownership.md), [0004](../decisions/0004-runtime-scope-shared-identities-and-navigation.md), [0006](../decisions/0006-rendering-and-resource-discipline.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md), [0015](../decisions/0015-adopt-fluid-functionalism-base-ui.md)    | Markdown Rendering; Document Viewers; File Transactions; Renderer Workspace                                        | `SourceReference`; folder-scoped `DocumentTabsRuntime`; independently disposable `DocumentRuntime`; active-tab `DocumentNavigationRuntime`; app-owned open-document and document-command composition; versioned `DocumentSourceApi` query/save/overwrite adapter; bounded `GenericFilePreviewApi`; versioned `DocumentAssetApi`; validated `MediaApi`; document-owned live draft, save barrier, conflict, Find, outline, anchor, JSON presentation, and PDF page authority; lazy retained Milkdown, PDF, image, DOCX, sandboxed HTML, and media surfaces; source-authoritative JSON Tree/Source surface; shared TXT/generic CodeMirror surface; validated external navigation and folder-scoped Reveal | Task 28's strict v1 session still receives only active-folder-relative tab identities; Tasks 30–40 reuse the legacy read/save, Milkdown, JSON, bounded generic-preview, folder-scoped asset, prepared DOCX, HTML bootstrap, media-preview, and transcript authorities through replacement-owned adapters and runtimes; generic preview remains read-only and excluded from retrieval; external navigation and Reveal remain explicitly authorized | Building     | Tasks 29–40 evidence in [Subphase 3](subphases-3-documents.md) covers document lifecycle, editing, save/conflict recovery, navigation, source-preserving JSON, generic code preview, and lazy bounded PDF, image, sanitized DOCX, sandboxed HTML, audio, and video viewing. Final J03 journey, accessibility, and performance evidence remains deferred to finalization.                                                                                                                                                                                                                                           |
| Preparation                  | J04      | Change / status is explanation, actions refetch; [0002](../decisions/0002-feature-runtime-and-protocol-ownership.md), [0004](../decisions/0004-runtime-scope-shared-identities-and-navigation.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md) | Data Lifecycle; Document Viewers; File Transactions; Settings and Config | Folder-explicit `PreparationStatusApi` over registered index-status schemas; `FolderIndexStatus` shared domain snapshot; pure source readiness, tree marker, and status-line projections; `PreparationControlApi` over registered prepare, reprocess, and cancel schemas; `usePreparationActions`; document `renderPreparation` and `onOpenPrepared` slots; file-tree `rowMarkers` and Reprocess context menu; sidebar attention cue; typed Electron `capture` capability, preload, and monitor; `ClipboardOffer` composition over `UploadApi`; General and Transcription Settings sections | Tasks 43–44 poll one folder-scoped status query nested under the workspace folder key; pending rows stay unmarked in the tree; failed, cancelled, and blocked rows are marked; actions never predict status; clipboard capture fails closed and is suppressed while an Agent composer owns focus; derived notes are never listed or opened | Building     | Tasks 43–44 focused protocol, readiness, adapter, hook, viewer slot, tree, sidebar, capture, Settings, Electron boundary, and renderer architecture evidence is listed in [Subphase 4](subphases-4-retrieval-and-preparation.md). J04 evidence through the real preparation path and the packaged capture check remain deferred to finalization. |
| Search and source navigation | J05      | Change / selected-workspace-only navigation; [0002](../decisions/0002-feature-runtime-and-protocol-ownership.md), [0004](../decisions/0004-runtime-scope-shared-identities-and-navigation.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md), [0015](../decisions/0015-adopt-fluid-functionalism-base-ui.md)                                | Data Lifecycle; Renderer Workspace; Settings and Config; MCP Access                                                | Active-folder `QuickOpenSource`; selected-folder occurrence-level exact-search navigation intents; `ExactSearchApi` and registered exact-search HTTP schemas; cancellable `useExactSearch` query boundary; document-owned pending `DocumentSearchTarget`; `ExactSearch`, `WorkspaceQuickOpen`, and `WorkspaceExactSearch` app composition; existing document-open, Find, and Reveal authorities; `SemanticSearchApi` and `IndexDecisionApi` over registered semantic-search, index-status, and embedder schemas; pure semantic readiness, grouping, and navigation-intent projections; `LibrarySearch` Exact/Similar surface and `WorkspaceSearch` composition; AI Index Settings section over `EmbedderPort`                                                                                                                                                                                                                                                                                                        | Tasks 41–42 reuse validated listing, `POST /api/library/keyword-search`, document-open, Find, and Reveal paths. Exact search is always bound to the selected Library folder and discards out-of-folder results; generic files remain excluded; Cmd/Ctrl+P opens Quick Open, Cmd/Ctrl+Shift+F always focuses Search, and Cmd/Ctrl+O remains unassigned. Task 45 reads AI Index readiness from the Task 43 status query, defaults Similar scope to the selected folder with a whole-library option, sends no semantic request while readiness forbids it, and receives folder-qualified hit identities from the server                                                                                             | Building     | Tasks 41–42 focused protocol, ranking, cancellation, selected-workspace filtering, evidence, keyboard/focus, app-composition, occurrence navigation, Reveal, and renderer architecture evidence is listed in [Subphase 4](subphases-4-retrieval-and-preparation.md). Task 45 focused server identity, protocol, adapter, readiness, grouping, mode and scope, Settings, and retrieval server-suite evidence is listed there too; final J05 journey, accessibility, and performance evidence remains deferred to finalization.                                                                                                                                                                                                              |
| Agent sessions and Chat      | J06      | Retain / [0002](../decisions/0002-feature-runtime-and-protocol-ownership.md), [0004](../decisions/0004-runtime-scope-shared-identities-and-navigation.md), [0007](../decisions/0007-failure-recovery-and-test-foundation.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md), [0015](../decisions/0015-adopt-fluid-functionalism-base-ui.md) | Agent Panel; Agent Runtime; MCP Access; Settings and Config                                                        | Pure session/workspace transitions and mounted-session ownership; selected-folder Chats navigator with a quiet New chat command and one bounded, date-grouped conversation tree that merges mounted/native sessions and omits content-free allocations; independently cancellable Agent history; transport-free blank drafts with composer-owned provider choice and a first-use start boundary; runtime-native model and thinking selection; bounded replay transcript; headerless lazy workspace with retry; normalized history, replay, mutation, and WebSocket ports; main-owned window attribution; session-owned composer draft, queue, turn, retry, permission mode, approval, and tool-activity transitions; bounded inert tool presentation; bound context items over registered context-file and attach schemas, `@` mention listbox, `/` skill catalog with a one-turn armed token, source drops, transient uploads, readiness chips, and send-time stale rejection; one file-change shape across runtimes with read-only unified diffs behind write decisions and tool rows, settled native diffs, a changed-files list with explicit Open, and post-write listing, document, and sync refresh | Tasks 47–51 reuse native Agent history, turns, tools, permissions, and adapters behind strict replacement schemas. The selected Library folder is the sole visible Chat scope; started work from another folder stays mounted but hidden and never rebinds. History remains browsable when an Agent is unavailable, readiness gates first-use send, permission decisions target only the matching pending request, and folder retirement cancels rather than widens pending work. Task 50 keeps the text-only prompt contract and the `Attached files:` suffix, with an armed skill sent as its id for the server to compose; the shell publishes the selected folder's listing and readiness so the Agent feature never imports Preparation or Workspace. Task 51 keeps write authority on the server: the renderer shows diff evidence, reports settled writes, and refreshes through the shell; it never selects Agent output and never saves on the Agent's behalf. Electron authorizes the socket and supplies window identity. Legacy UI and routes remain until cutover.                      | Building     | Focused domain, runtime, component, adapter, protocol, and Electron tests cover session lifecycle plus first-use send, retained drafts, runtime-advertised model and thinking selection, queued follow-ups, streaming, interruption, retry settlement, count-free tool activity, bounded human-readable arguments/results, live permission modes, explicit approval/rejection, late-event guards, permission focus recovery, scope-safe cancellation, mention ranking and editing, skill catalog arming, one-turn spend, and retry, context validation and wire rendering, stale-send refusal, queued snapshots, file-tree source drags, file-change extraction and settled listing, native diff recording, read-only unified diffs and patches, changed-file Open routing, post-write document and folder refresh, and the folder sync adapter. Final J06 and J07 journey, accessibility, performance, and platform screen-reader evidence remains deferred to finalization.                                        |
| Chat-to-document convergence | J07      | Retain / [0004](../decisions/0004-runtime-scope-shared-identities-and-navigation.md), [0007](../decisions/0007-failure-recovery-and-test-foundation.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md) | Agent Panel; MCP Access; File Transactions; Markdown Rendering | Document tabs as source drag producers beside file-tree rows; open-document paths published with the Agent scope environment and leading the `@` ranking; the existing permission card with diff evidence, server-applied write, post-write listing, document, and sync refresh, explicit changed-file Open, and Task 33 reconcile and conflict decisions | Task 52 adds no write surface and no Canvas file type: convergence stays a prompt whose write is approved and applied on the server through the shared versioned save. Nothing references the open document implicitly; a tab drag or an `@` pick is the user's gesture. The transcript is never copied into a source. J07 journey evidence is a driven runtime pass recorded at finalization. | Building     | Focused tab drag, mention ranking, composer-focus, and J07 composition tests (listing refetch without focus theft, explicit Open, clean adoption, dirty conflict) plus typecheck, lint, architecture, and web build. J07 accessibility and real-runtime Canvas write evidence remain deferred to finalization. |
| External Agent access | J08 | Retain / [0002](../decisions/0002-feature-runtime-and-protocol-ownership.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md), [0015](../decisions/0015-adopt-fluid-functionalism-base-ui.md) | MCP Access; File Transactions; Data Lifecycle; Settings and Config | Settings-owned `McpAccessPort` over registered MCP status, token-rotation, Docker-access, and Docker-port schemas; pure `McpAccess` read model with the `off`/`starting`/`active`/`failed` listener projection; one MCP Settings section on the Task 46 row grammar with copy, masked token, confirmed rotation, and Docker opt-in; the existing MCP transports, operations, and format-capability enforcement stay server-owned | Task 53 reuses the legacy status and HTTP-settings routes unchanged and adds no write beyond token rotation and the Docker opt-in and port; the section is read-only configuration guidance, never a client-config writer; credentials stay in Settings and are never persisted by the renderer; legacy UI and routes remain until cutover | Building | Task 53 focused domain projection, protocol schema, adapter, hook ordering-guard, component, story accessibility, architecture, typecheck, lint, format, and renderer build evidence in [Subphase 6](subphases-6-external-reporting-and-recovery.md). Operation parity, transport, authorization, and format-capability evidence stays with `pnpm test:mcp`. Packaged launcher, copied configuration, and third-party client behavior remain release evidence. |
| Bug-report review | J09 | Retain / [0007](../decisions/0007-failure-recovery-and-test-foundation.md), [0010](../decisions/0010-sandboxed-renderer-and-isolated-build.md), [0012](../decisions/0012-persistence-commands-and-contract-evidence.md) | Bug Reporting; Window Lifecycle; Architecture | Second replacement page `bug-report.html` served over `app://` in a sandboxed, context-isolated window with its own bundled preload; shared review and open schemas under `shared/protocols/electron/`; one `ReviewSession` reducer over `loading`, `unavailable`, `reviewing`, `preparing`, `ready`, and `closed` with generation-stamped completions; `BugReportReviewPort` adapter over the authorized review IPC; a `bugReport` window capability behind the sidebar Report a bug entry and the native Help entry | Task 54 keeps every authority in main: draft service, collection, redaction, approval, and handoff modules are unchanged, review IPC is registered inside the replacement boundary with sender authorization, and main parses each response against the shared schema before it crosses. The legacy review page still serves the dev-Vite path and is removed at cutover | Building | Task 54 focused protocol, domain, runtime, adapter, hook, component, story accessibility, platform bridge, Electron preload/IPC/window, app-protocol, boundary smoke (both pages), architecture, typecheck, and web build evidence plus a driven runtime pass in [Subphase 6](subphases-6-external-reporting-and-recovery.md). Collection, redaction, approval, and handoff evidence stays with the existing `electron/bug-report-*.test.cjs` suites. Packaged capture, Downloads copy, and browser handoff remain release evidence; J09 journey evidence is deferred to finalization. |
| Durable core loop            | J10      | Unresolved / —                                                                                                                                                                                                                                                                                                                                                        | Renderer Workspace; Data Lifecycle; Agent Runtime; Agent Panel; MCP Access; File Transactions; Markdown Rendering  | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Not assessed | Populate before Phase 6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Conversation to project      | J11      | Unresolved / —                                                                                                                                                                                                                                                                                                                                                        | Renderer Workspace; Settings and Config; MCP Access; Agent Runtime; Agent Panel; File Transactions; Data Lifecycle | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Not assessed | Populate before Phase 6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Search-by-meaning onboarding | J01 | Change / the folder-scoped skip is replaced by one durable invitation; `main` deleted both accessors and the replacement adopts the new model rather than the old one. Adopted by [0019](../decisions/0019-one-time-search-setup-invitation.md). | Renderer Workspace; Settings and Config | First-folder activation offers hosted or bring-your-own-key setup once; the By meaning search mode, a persistent Files-panel action, and Settings remain the manual routes back | Persisted preference changes shape from per-folder to application-level, so a stored folder-scoped skip is not migrated and is ignored; declining never costs local functionality | Specified | Task 66 owns implementation and the J01 rewrite. Blocker: none. |
| Hidden entry visibility | J02, J03 | Retain / the server keeps classification authority and the renderer owns only the toggle and the echoed state | Renderer Workspace; Data Lifecycle; File Transactions | A Settings-owned port over the workspace-preferences read and write routes; the workspace listing carries the applied visibility as a required field so no window trusts its own optimistic toggle | The listing wire gained a required applied-visibility field at task 63; an explicit member listing is Agent-facing and always reports false, so surfaced rows never widen Agent discovery | Building | Task 67 landed the tree control, the applied-visibility echo, the guarded write, and the Quick Open and open-tab guarantees. Exact evidence is listed in [Subphase 8](subphases-8-main-convergence.md). |
| Public repository import | J02 | Retain / the server owns acquisition and publication; the renderer owns validation feedback and the request lifecycle only | File Transactions; Renderer Workspace; Data Lifecycle | A folder-explicit import request from the Library switcher and folder-add menu, with canonical owner and repository parsing, an auto-derived folder name, and a live destination preview under folder home | Additive route; the renderer never learns a staging path, and a failed, cancelled, or refused import leaves existing members untouched | Building | Task 68 landed the two entry points, the shared URL and destination-name rules read through the port, code-selected refusals, and cancellation. Acquisition, submodule and Git LFS refusal, and staging cleanup pass the github-import server suite. Exact evidence is listed in [Subphase 8](subphases-8-main-convergence.md). |
| Agent Instructions | J06 | Retain / a packaged default plus an optional saved customization per scope, injected verbatim by the runtime adapters | Agent Runtime; Agent Panel; Settings and Config | A Settings-owned port over the agent-instructions read and write routes; one exported scope-key helper keeps the request parameter and the effect identity from disagreeing; the editor and its presence indicator follow the active Chat's scope | Folder and Library scopes each resolve their own key; the renderer never holds a resolved prompt as durable state and never writes an instruction file into a source folder | Specified | Task 69 owns implementation. Resolution and injection already pass `pnpm test:config` and `pnpm test:agent` after the merge. |
| Product vocabulary | J01, J05, J06 | Change / AI Index retires in favor of search by meaning, Source file becomes Source, and search credits separate from the Wiki Agent's seven-day allowance | Settings and Config; Renderer Workspace; Agent Panel | Renderer copy, stories, and focused tests only; wire field names stay as the merge left them, so a rename that would cross the wire needs its own decision | No persisted or wire change. Retrieval failure messages, readiness titles, and the Similar mode's unavailable title still carry the retired term until the sweep lands | Specified | Task 65 owns the sweep. Blocker: none. |
| Wiki Pages | J12 | Retain / building a wiki is an ordinary visible Agent request against the active folder, not a staged intent machine | Agent Panel; Agent Runtime; File Transactions | The existing composer, permission, and server-applied write path; Agent Instructions carry the durable contract; the resulting pages land as ordinary local source files | No new file type and no private write surface. `main`'s staged Build Wiki intent machinery is not carried across; the observable outcome is reached through the ordinary request path | Specified | Task 72 owns implementation and the J12 evidence. Journey and coverage sections arrived with the merge; this row is the ledger's first record of it. |
| Gallery | J13 | Retain / the shop is a real product surface, and its egress design already suits the replacement | Agent Panel; Renderer Workspace; Architecture | The daemon's pinned-host index and image proxy; a bare window derives the band below its blank Chat composer and a folder window opens the same shop as an overlay from an existing sidebar entry; Make a copy reuses the public repository import into folder home | The renderer never reaches an external host directly, which is what the strict `app://` policy requires. Total index failure answers with a success envelope carrying an unsupported schema and the client falls back to the bundled snapshot | Specified | Task 73 owns implementation and the J13 evidence. The proxy guard already passes `pnpm test:library-files` after the merge. |
| Launcher and composer chrome | J06 | Remove / the replacement keeps its own Fluid composition; moving agent choice into a composer pill, regrouping the sidebar launchers, and retiring kind tabs are legacy-renderer decisions that the rebuild already answers differently | Agent Panel; Renderer Workspace | None. Agent choice, scope selection, and chat creation keep the replacement's existing surfaces | None. The behavior stays in the inert legacy reference and never reaches the replacement | Specified | Refused at task 64 and recorded so the omission reads as a decision. A later decision may adopt any part of it on the replacement's terms. |
| Resizable outline dock | J03 | Remove / the replacement's document outline keeps its current composition | Renderer Workspace; Document Viewers | None | None. The behavior stays in the inert legacy reference | Specified | Refused at task 64. Reopen with a decision if the fixed dock proves too narrow in use. |
| Agent-panel and retrieval behavior fixes | J05, J06 | Remove / per-agent history streaming, stale-failure dismissal with retry attribution, chat-scope retrieval defaulting, and the creation-control reachability fix are repairs to legacy-renderer implementations the replacement does not share | Agent Panel | None. The replacement's session, failure, and retrieval runtimes are separate implementations | None. Any equivalent defect in the replacement is its own bug, found against its own code | Specified | Refused at task 64. Each is worth re-checking against the replacement's runtimes during tasks 57 and 58 rather than ported. |

## Cross-Cutting Gates

| Gate                                   | Status       | Evidence / decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Dependency boundaries                  | Proven       | `pnpm test:renderer-architecture` proves graph direction, cycles, feature isolation, layer APIs, legacy isolation, and wire-schema registration against the replacement graph.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Runtime contract validation            | Building     | Shared Zod schemas cover completed renderer, library, document, workspace-session, and Agent history/WebSocket boundaries. Protocol, Electron, server, adapter, and live `app://` tests prove those entries; remaining protocols arrive with their owning slices.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Accessibility and keyboard operation   | Building     | Task 26 `FileTree` component evidence proves tree/treeitem semantics, one roving tab stop, collapsed-row omission, semantic levels/positions, and Up/Down/Home/End/Left/Right keyboard behavior against the replacement. Task 35 adds semantic outline controls, current-heading state, Find status announcements, and active-document Ctrl/Cmd+F, Ctrl/Cmd+G, Shift+Ctrl/Cmd+G, and Escape handling. Task 36 extends those contracts to the JSON tree with roving focus, structural expansion, selection, and active-document Find across the synchronized Tree and Source panes. Task 37 adds named read-only code surfaces, keyboard Find, and explicit retry/Reveal recovery. Final axe, forced-colors, 200% scaling, platform screen-reader, and journey evidence remains deferred to finalization. |
| Theme, scaling, reduced motion         | Building     | Decision 0015 adopts Fluid Functionalism and its Base UI flavors. `renderer/src/app/providers.tsx` mounts the Fluid shape, size, surface, icon, tooltip, and reduced-motion providers; `renderer/src/globals.css` and the bundled Inter Variable and Geist Mono Variable fonts carry the production theme. Typecheck and build prove integration only. Representative theme, shape, size, focus, motion, and final product visual evidence remain Tasks 12–16. User-chosen theme, interface size, and reading text size arrive with the Appearance Settings section in Task 61; applying them before first paint stays with Task 59.                                                                                                                                                                                                                                                                                                                                               |
| Initial JavaScript and lazy entries    | Not assessed | Record isolated replacement baseline in Phase 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Startup and interaction performance    | Not assessed | Record measurement method in Phase 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Electron titlebar and window lifecycle | Building     | Boundary, request-authorization, multi-window, and live smoke tests cover the secure `app://` preload boundary, HTTP/Agent-socket attribution, window policy, active-folder registration, save-gated close/reload, removal, and workspace-session restoration. Final journey evidence remains deferred.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Privacy and credential boundary | Building | Settings sections read hosted, embedding, transcription, Agent runtime, and MCP credentials only through Settings-owned ports; the MCP token is masked by default and never persisted by the renderer (Task 53). Agent permissions and writes stay server-approved (Tasks 49–51). The bug-report window reads only service-produced safe previews through schema-parsed IPC and submits nothing (Task 54). Recovery drafts are encrypted with an Electron-provisioned key that the server scrubs from its environment, live outside library folders, and are disabled without OS-protected storage (Tasks 55–56, Decision 0017 pending acceptance). Packaged credential storage and platform keychain evidence remain release checks. |

## Update Rule

Every migration PR updates only the rows it changes. A row moves to Proven
only when its focused evidence runs against the replacement entry. Record an
unresolved requirement as a gap with its owning contract; do not soften the
requirement or describe incomplete behavior as parity.

Any temporary compatibility adapter is named in the final column with its owner
and deletion condition. A changed or removed behavior links its approved
decision; confidence or a broad command is never a substitute for exact
evidence.
